# AGENTS.md

1. Любую задачу начинай с чтения `game_overview.md`. Если в постановке задачи есть расхождения с game_overview - спрашивай, нужно ли доработать game_overview в соответствии с новыми данными или пользователь ошибся, и тогда нужно скорректировать постановку задачи в соответствии с game_overview.
2. При составлении плана всегда делай это с расчетом на то, что план составляется для джуниор-разработчика, которому нужно подробно объяснить все шаги и критерии успешности.
3. Если `cargo` не найден в `PATH` - используй `C:\Users\andreybotanic\.cargo\bin\cargo.exe`
4. Крупнае изменения обязательно должны иметь тесты.
5. После завершения любой задачи:
    - все значимые изменения, касающиеся логики игры (не технические детали) фиксируй в `game_overview.md`;
    - выполняй `cargo build --release`;
    - запускай релизную версию и проверяй логи - приложение должно запускаться, а в логах не должно быть ошибок;