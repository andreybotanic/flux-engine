[package]
name = "flux_plugin_sdk"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy_math = { version = "0.16", features = ["serialize"] }
flux_plugin_abi = { path = "../flux_plugin_abi" }
semver = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
smallvec = "1.13"
