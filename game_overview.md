# Игра FluxEngine (название временное)

## Концепт

Жанры: песочница, симулятор, строительство, выживание.

Игровой мир наполнен разными газами, жидкостями, ископаемыми породами, минералами, растениями иживотными.

Игра 2D, вид "сбоку", т.е. экранные направления вверх/вниз/влево/вправо соответствуют аналогичным игровым направлениям. Ось вперед/назад отсутствует.

## Симуляция

Распространение жидкостей и газов реализуется клеточными автоматами. Реализация клеточных автоматов должна использовать максимальный параллелизм.

Весь игровой мир накладывается на квадратную сетку, в рамках которой происходит вся симуляция.

### Текущий статус MVP

На текущем этапе реализуется первый рабочий каркас игры на Rust + Bevy + wgpu/WGSL.

В MVP реализованы:
- мир размером 102x102 клетки
- непроницаемая рамка по периметру мира
- базовый интерфейс просмотра мира
- разделённые меню:
  - главное меню (без загруженного мира): `New Game / Load / Exit`
  - игровое меню (при загруженном мире, по `Esc`): `Continue / Save / Exit to Main / Exit`
- панорамирование и масштабирование камеры
- компактный полупрозрачный HUD состояния клетки рядом с курсором
- управление скоростью симуляции (x1, x2, x5) и пауза из UI и с клавиатуры
- отдельный режим отображения газов
- полупрозрачные линии сетки (поверх всех слоёв, z=1.5, alpha≈0.09)
- отладочный режим (клавиша `` ` ``): авто-пауза, пошаговое выполнение клавишей Enter, зелёная рамка (активная клетка) и синяя рамка (выбранный сосед) для следующего шага диффузии

Симуляция в MVP выполняется с фиксированным шагом 30 Гц.

Поверх фиксированного шага добавлен управляемый множитель скорости:
- x1, x2, x5
- переключение скоростей кнопками интерфейса и клавишами < и >
- пауза кнопкой интерфейса и клавишей Space
- старт игры по умолчанию в состоянии паузы

## Газы

Газы имеют следующие характеристики:
- атомная масса
- цвет (может быть бесцветным)

Симуляция газов должна предусматривать смешивание разных газов в одной клетке.

Газу должны вести себя как в реальности:
- заполнять всё предоставленное им пространство
- не проходить через твердые непроничаемые для газа клетки
- легкие газы должна подниматься вверх, а тяжелые - опускаться вниз

Должны быть предусмотрены особые клетки, взаимодействующие с газом:
- источники газа: добавляют определенное количество газа в клетку
- потребители газа: удаляют определенное количество газа из клетки

### Текущий статус MVP газов

На первом шаге реализуется только один газ: водород.

Для первого рабочего прототипа используется только диффузия газа по окрестности фон Неймана порядка 1.

На текущем шаге MVP газовая симуляция работает только в unified TRT-модели:
- внутреннее хранение концентраций H2/O2 — `f32`; UI-инструменты редактирования по-прежнему принимают целые значения количества газа
- перенос видов газа, макрополе плотности и скорость считаются в одном unified LBM-шага без отдельного legacy pair-diffusion прохода
- mass-fix выполняется периодически и/или по событию превышения ошибки массы; residual больше не привязывается к «первой открытой клетке», а распределяется по уже занятым клеткам вида
- reconciliation total-density выполняется периодически (и по событию mass-fix), что снижает численные артефакты при длинных прогонах
- buoyancy рассчитывается контекстно по локальной среде (окно 5x5 с гауссовыми весами): вклад каждого газа определяется сравнением его молекулярной массы с локальной средней массой смеси и нелинейной функцией; параметры вынесены в `SolverTuning` для live-тюнинга
- в debug-overlay доступно отображение импульса (не скорости) стрелками из центров клеток; для малых экранных длин рисуется точка вместо стрелки
- в `Debug Panel` добавлены runtime-метрики шага (`last/avg step ms`, `actual Hz`, `target effective Hz`) и настройка базовой частоты `Simulation Hz` (1..1000)
- кнопки `x1/x2/x5` сохранены как множитель к базовой частоте; если производительности недостаточно, фактическая частота автоматически ограничивается максимально достижимой
- базовые игровые данные вынесены в TOML-конфиги (`config/simulation.toml`, `config/cell_types.toml`, `config/gases/*.toml`); газы загружаются из отдельных файлов, что позволяет расширять набор газов без правок кода
- внутренняя CPU-симуляция газа переведена на динамический набор видов (без фиксированной привязки к H2/O2/CO2): количество газов определяется конфигами
- добавлен третий дефолтный газ `CO2` (углекислый газ) через конфиг
- в основном виде F1 добавлена цветная визуализация газа: цвет клетки смешивается по долям газов (взвешенный mix), интенсивность зависит от общей концентрации

Подъем легких газов вверх и опускание тяжелых газов вниз теперь обеспечиваются buoyancy-моделью и настраиваются параметрами `SolverTuning`.

Визуализация MVP газов:
- основной слой F1: обычный вид мира, водород визуально прозрачный
- слой газов F2: мир и фон отображаются через серый фильтр, концентрация газа показывается насыщенностью цвета клетки; текстура overlay на текущем этапе синхронизируется напрямую из CPU-состояния газа, слабые концентрации усилены визуальной кривой

Техническая фиксация для корректного обновления overlay:
- текстуры газа создаются с `MAIN_WORLD | RENDER_WORLD`, чтобы изменения цвета из main world корректно попадали в рендер
- добавлены unit-тесты на инициализацию газа, блочно-синхронную активацию, сохранение суммарного количества частиц и равномерность активации при циклическом сдвиге


## Жидкости
Not yet

## Ископаемые породы и минералы
Not yet

## Растения
Not yet

## Животные
Not yet
## Changelog
- 2026-05-02: Разделено меню на два независимых режима. `Main Menu` (когда мир не загружен) теперь содержит только `New Game / Load / Exit`, а `Game Menu` (при активном мире, по `Esc`) — `Continue / Save / Exit to Main / Exit`. `Esc` больше не может закрыть главное меню без мира.
- 2026-05-02: Добавлен полный confirm-flow для несохранённых изменений при `Exit to Main` и `Exit`: диалог `Save / Don't Save / Cancel`; при выборе `Save` после успешного сохранения автоматически выполняется исходное действие.
- 2026-05-02: При `has_world = false` скрывается world-зависимый UI (панели инструментов и панель скорости), фон за главным меню остаётся пустым, а симуляция не запускается до `New Game` или `Load`.
- 2026-05-02: Уточнён UX стартового состояния без мира: до `New Game`/`Load` кнопка `Continue` не показывается, `Esc` не закрывает `Main Menu`, а значит выйти из меню в симуляцию без созданного/загруженного мира нельзя.
- 2026-05-02: Поведение старта мира уточнено: при запуске мира теперь нет в активном состоянии (`no world loaded`), фон за Main Menu пустой, а запуск симуляции заблокирован до `New Game` или `Load`. Кнопки/Space не снимают паузу, пока мир не создан/не загружен.
- 2026-05-02: Добавлена полноценная система сохранений мира в `saves/<save_id>/` и обновлено главное меню. `Save` поддерживает создание нового слота (display name до 64 символов) и overwrite существующего слота с обязательным подтверждением полной перезаписи. `Load` показывает список слотов (сортировка new->old), запрашивает подтверждение при наличии несохраненных изменений и после загрузки полностью заменяет состояние мира, оставляя игру на паузе. В сохранение включены клетки, все виды газа, скорость, total-density, внутренние LBM-буферы и `simulation_step`, что дает точное продолжение симуляции.
- 2026-05-02: Boot-путь мира переведен на save-first логику: `config/world.toml` и fallback инициализации мира из конфига удалены. Стартовый мир теперь пустой (границы + пустые клетки/газы), а признак `unsaved changes` считается только по правилу `simulation_step > last_persisted_step` (т.е. только после реального шага симуляции).
- 2026-05-02: Исправлена диагностика `Mass err` и семантика шага симуляции: ручные редакторские операции с газом/клетками больше не увеличивают `SimulationStep` (счётчик теперь отражает только реальные шаги солвера), обновление газовых overlay переведено на реакцию на изменения `GasField`, а для `Mass err` при очень малой базовой массе вида включен абсолютный режим ошибки (вместо взрывного относительного), чтобы исключить ложные значения порядка сотен тысяч после ручных правок состава газа.
- 2026-05-02: Стартовая инициализация газов отделена от конфигов газов: параметр `initial_center_particles` удален из `config/gases/*.toml`, добавлен отдельный конфиг мира `config/world.toml` с начальными размещениями газа (`[[gas_placements]]` с `gas_id`, координатами/размером, `amount`, `replace`), а создание `GasField` переведено на пустой старт с последующим применением world-конфига на startup. Обновлена debug-метрика `Mass err`: для видов с нулевой базовой массой теперь считается абсолютная ошибка (а не принудительный 0), baseline автоматически переинициализируется при ручных правках газа без шага симуляции.
- 2026-05-02: Добавлено численное подавление незатухающих волн в unified TRT-солвере: в `SolverTuning` введены параметры `velocity_damping` (демпфирование макроскорости) и `species_eq_blend` (сглаживающее смешивание transport-весов вида с локальным equilibrium), применяемые для стабилизации переноса газа в сложной геометрии. В конфиг `config/simulation.toml` добавлены соответствующие поля в секции `[solver_tuning]`; добавлен регрессионный тест `wave_noise_decays_in_obstacle_world`.
- 2026-05-02: Уточнена конфигурация газов и F1-визуализации: реестр газов теперь упорядочивается по молекулярной массе (для дефолтного набора `H2 -> O2 -> CO2`), а порог концентрации для максимальной интенсивности в основном виде F1 вынесен в отдельный параметр `visual.f1_max_particles_for_max_intensity` в `config/simulation.toml`.
- 2026-05-02: Исправлена регрессия численной стабильности газовой симуляции после перехода на динамический реестр газов: в `renormalize_species_mass` возвращен двухпроходный mass-fix с финальным exactness-pass (точное закрытие остатка без corner-артефактов), добавлена синхронизация `sync_lbm_from_total_density` для стабилизационных сценариев и восстановлены регрессионные тесты на long-run устойчивость/finite-значения/нулевой паразитический дрейф для равномерной смеси из 3 газов.
- 2026-04-29: Added `.gitignore` for the Rust/Bevy project to ignore build artifacts (`target/`), IDE folders, and OS-generated temporary files.
- 2026-04-29: Implemented world editing toolbars and editor workflows (bottom main toolbar with Build/Erase, top debug toolbar with Add Gas/Clear and toggle visibility, right gas settings panel with amount/replace), drag-based solid painting/erasing, rectangle gas apply/clear, blueprint and selection overlays, and erase cursor marker. Solid cells are now truly impermeable for gas simulation and wall visuals update dynamically when grid cells change.
- 2026-04-29: Fixed Bevy runtime system-param conflict (`B0001`) in editor UI systems by separating mutable `Visibility` and `Text` accesses with `ParamSet`, including cursor overlay updates, eliminating startup panic.
- 2026-04-29: Improved editor UX: made Build blueprint more transparent, added per-cell destroy highlight for Erase tool, replaced gas amount +/- controls with a direct numeric input field (keyboard digits + Backspace + Enter/Escape), and configured Bevy `AssetPlugin` to use absolute `<project>/assets` path to remove runtime shader path errors when launching release binaries outside project root.
- 2026-04-30: Refined editor interaction: Build blueprint now appears only while LMB is not pressed and uses lower transparency; Erase now highlights the exact target cell under cursor; gas amount control switched to keyboard numeric input with visible caret/focus state; gas overlay now refreshes immediately after editor gas mutations by bumping `SimulationStep`; asset loading remains pinned to absolute project `assets` path to avoid release-run shader path errors.
- 2026-04-30: Fixed gas amount input caret rendering in editor UI: while focused, the field now explicitly shows a visible text cursor (`|`) at the input end.
- 2026-04-30: Reworked gas amount entry into a reusable full-featured text input subsystem (`ui/input_field.rs`): supports cursor placement by mouse click, ArrowLeft/ArrowRight/Home/End navigation, Backspace/Delete editing, per-field allowed-character policy, and pluggable parser to internal value (`String` or `u32` range). Editor gas amount now uses this shared input field component.
- 2026-04-30: Fixed `B0001` panic in reusable text input system by splitting conflicting `TextInputField` queries in `focus_text_input_on_click` into a `ParamSet` (read query for click detection + separate mutable query for focus/cursor updates).
- 2026-04-30: Replaced symbolic text-input caret with a real UI caret entity in ui/input_field.rs: caret is now rendered between characters (not as part of the text), supports mouse placement between glyphs and arrow-key movement, and blinks like a standard input control.
- 2026-04-30: Газовая симуляция переведена на многокомпонентный клеточный автомат (H2 + O2): добавлены общая фаза LBM (скорость/направление), перенос всех газов общим полем скорости, диффузия сразу по всем газам в активированных клетках и обязательная синхронизация total density = H2 + O2 перед новым шагом LBM. В редакторе добавлены переключатели фаз `LBM/Velocity` и `Diffusion`, выбор типа газа в Add Gas, а HUD под курсором теперь показывает H2/O2/Total.
- 2026-04-30: Изменена инициализация и добавление газа: новая масса теперь раскладывается по D2Q9-направлениям с нулевой суммарной скоростью (центр 4/9, оси по 1/9, диагонали по 1/36), а при количестве не кратном 36 остаток распределяется по тем же вероятностям в случайные направления. Для Add Gas/Clear Gas LBM обновляется локально через delta массы в клетках без глобального ресинка, что сохраняет импульс существующего потока.
- 2026-04-30: Исправлено распространение газа при отключенной диффузии: перенос видов газа теперь выполняется по направленным долям post-collision D2Q9 в фазе LBM, поэтому стартовая клетка корректно раздает массу в соседние клетки даже при `Diffusion: Off`. Добавлен интеграционный тест на этот сценарий.
- 2026-04-30: Добавлен индикатор состояния отладочного режима рядом с кнопкой Debug tools: строка `Debug tool: on/off` теперь отражает состояние режима, переключаемого клавишей `~` (`` ` ``).
- 2026-04-30: Переработан отладочный UI: убраны индикаторы состояний `Debug tool`/`Debug tools`; панель инструментов Add Gas/Clear Gas теперь показывается только при активном DebugMode (клавиша `~`/`` ` ``). Настройки фаз вынесены в отдельную `Debug Panel` с заголовком, а выбор газа и количества — в отдельную панель `Gas Tool`, которая видна только при активном инструменте Add Gas. В `Debug Panel` добавлен переключатель показа клеток диффузии, он отображается только при включенной фазе Diffusion и управляет визуальной подсветкой этих клеток.
- 2026-04-30: Усилен визуальный контраст карты газов F2: кривая отображения концентрации изменена так, чтобы минимальные концентрации (например 1 частица) были почти незаметны, а различия между низкими и высокими значениями читались лучше.
- 2026-04-30: Подкручена кривая контраста F2 к промежуточному значению между «сильным усилением слабых концентраций» и «почти полным подавлением слабых концентраций», чтобы лучше балансировать читаемость средних и высоких концентраций.
- 2026-04-30: Обновлена визуализация F2 для низких концентраций: интенсивность теперь нормализуется по диапазону 1..1000 частиц (всё выше 1000 насыщается как 1000), а для любой ненулевой концентрации введён минимальный уровень видимости, чтобы слабый газ был заметен по всей сетке.
- 2026-04-30: В `Debug Panel` добавлено управление гаммой визуализации F2 (диапазон 0..10, ввод с `.` или `,`, до 3 знаков после разделителя). Изменение значения применяется сразу к gas overlay без ожидания следующего шага симуляции.
- 2026-04-30: В `Debug Panel` добавлен параметр `Max color at` (целое 1..10000): это порог частиц, начиная с которого цвет F2 считается максимальным. Порог применяется мгновенно, что позволяет различать низкие концентрации, когда фактический максимум на карте сильно ниже стандартного насыщения.
- 2026-04-30: Выполнен переход газовой симуляции на hybrid float-ядро: концентрации H2/O2 внутри `GasField` переведены на `f32`, диффузия заменена на антисимметричный pair-flux с limiter-ограничением доли переноса за substep, добавлен mass-fix (renormalization + residual correction) для стабилизации сохранения массы на длинных прогонах, а reconciliation LBM после диффузии переведен с жесткого full-reset в equilibrium на мягкое масштабирование текущих распределений. Обновлены отладочное preview и набор тестов численной устойчивости/сохранения массы.
- 2026-04-30: Исправлена семантика визуализации F2 после перехода на float-газ: overlay теперь использует округленное количество частиц для цвета/хранения, поэтому клетки с отображаемым `0` частиц больше не подсвечиваются из-за микроскопических float-остатков.
- 2026-04-30: Запущен research-spike по устранению волн через unified LBM: добавлены режимы `LegacyHybrid / UnifiedTRT / UnifiedMRTCM`, параметры `SolverTuning` (`tau_even`, `tau_odd`, `relaxation_rates[]`, `target_cfl_like_limit`), переключение режима солвера из Debug Panel и A/B-совместимость UI (`Diffusion` в unified трактуется как species-relaxation). В debug-панель добавлены метрики `anisotropy_score`, `radial_wave_score`, `mass_error_h2/o2`, а в тесты — интеграционные сценарии S1/S2/S3 и spike-сравнение unified-режимов с legacy.
- 2026-04-30: Реализован план v2 по устранению артефактов и направленному истечению: убран corner-anchor residual в mass-fix (остаток перераспределяется по занятым клеткам), добавлены пороговые фильтры debug-preview (`preview_min_amount`, `preview_min_flux`), введена buoyancy-сила в unified LBM (`enable_buoyancy`, `buoyancy_strength`) и UI-переключатель buoyancy в Debug Panel. Обновлена стратегия mass-fix/reconciliation (порог ошибки + периодический шаг). Добавлены тесты `mass_fix_does_not_inject_corner_artifact`, `preview_ignores_micro_flux`, `buoyancy_force_finite_and_bounded`, `unified_buoyancy_creates_upward_jet_in_hole_scenario`.
- 2026-04-30: В `Debug Panel` добавлен переключатель `Show impulses` для отображения/скрытия векторов импульса газа. В debug-overlay импульс рисуется стрелками из центров открытых клеток (линия + наконечник); от нормализованного модуля импульса зависят и длина хвоста, и размеры боковых отводов наконечника.
- 2026-05-01: Обновлена визуализация импульса в debug-overlay: длина стрелки теперь оценивается в экранных пикселях с учетом текущего зума камеры; если хвост стрелки короче 2 px, вместо стрелки рисуется точка в центре клетки. Это убирает визуальный шум от сверхмалых импульсов без фильтрации физики в симуляции.
- 2026-05-01: Порог переключения «стрелка/точка» для импульсов сделан масштаб-зависимым: при приближении камеры порог в пикселях уменьшается, поэтому короткие векторы можно рассмотреть как стрелки; при отдалении порог растет и подавляет микрошум.
- 2026-05-01: Цвет векторов импульса в debug-overlay изменен на желтый оттенок для лучшей визуальной читаемости на фоне карты газа.
- 2026-05-01: Толщина линий векторов импульса в debug-overlay уменьшена до `1.5 px` (было `2 px`) через отдельную gizmo-конфигурацию только для импульсов, чтобы не затронуть толщину других debug-оверлеев.
- 2026-05-01: Выполнен полный переход runtime-симуляции на unified TRT: удалены legacy-переключение solver mode и отдельный 3x3 diffusion-step из рабочего шага, обновлены UI-семантики (`Species relax` вместо legacy diffusion mode), а debug-preview legacy diffusion убран.
- 2026-05-01: В симуляцию добавлены runtime-метрики производительности (`last/avg step ms`, `actual Hz`, `target effective Hz`) и управление базовой частотой fixed timestep (`Simulation Hz` 1..1000) через debug-панель.
- 2026-05-01: Модель buoyancy обновлена до массово-зависимой: сила подъем/оседание считается по вкладам всех газов относительно опорной массы с нелинейной функцией; добавлены параметры `buoyancy_ref_mass`, `buoyancy_gain`, `buoyancy_alpha`, `buoyancy_force_cap` для live-тюнинга.
- 2026-05-01: Модель buoyancy переведена на локально-контекстную схему без фиксированной `buoyancy_ref_mass`: локальная среда оценивается в окне 5x5 с гауссовыми весами, сила считается через сравнение молекулярной массы газа с локальной средней массой смеси. В `SolverTuning` добавлены параметры `buoyancy_window_radius` и `buoyancy_window_sigma`, поле `buoyancy_ref_mass` удалено из runtime и debug UI.
- 2026-05-01: Подкручена buoyancy-модель для разделения смесей H2/O2: к локально-контекстной силе добавлен видоспецифичный вертикальный дрейф при species-relaxation (на основе buoyancy-коэффициента каждого газа), чтобы смесь не залипала в идеальном 1:1 распределении по клеткам. Обновлены дефолты тюнинга (`strength`, `gain`, `alpha`, `cap`) для более заметного разделения легкого и тяжелого газа.
- 2026-05-01: Исправлена причина незатухающих волн на двух газах: макро-buoyancy теперь считается от разности локальной средней массы смеси в клетке и окружении (`m_cell` vs `m_env`), поэтому при одинаковом составе клетки и среды сила близка к нулю. Видоспецифичный дрейф оставлен только как относительный (`species_b - mix_b`) и ослаблен, что уменьшает вертикальные автоколебания при сохранении разделения H2/O2.
- 2026-05-01: Добавлен переключаемый backend симуляции `CPU/GPU` (в runtime через `--sim-backend`/`FLUX_SIM_BACKEND`, горячее переключение клавишей `F8`). Для GPU backend реализован новый compute-solver на WGSL (`assets/shaders/gas_solver.wgsl`) и удален старый неиспользуемый шейдер `assets/shaders/gas_diffusion.wgsl` вместе с legacy compute-path. Добавлены runtime-метрики GPU шага (`compute/upload/readback/total`), отдельная CPU reference-модель для больших сеток (`102/502/1002`) и CLI-профилировщик `cargo run --release --bin gas_perf`, который генерирует отчеты `reports/gas_perf_report.md` и `reports/gas_perf_report.csv`. Добавлены тесты: parity CPU↔GPU (с допуском по численным расхождениям, опционально через `RUN_GPU_TESTS=1`), а также smoke-проверки на больших мирах.
- 2026-05-01: Расширены GPU parity-тесты: добавлен сценарий с псевдослучайной инициализацией газов на `102x102` и проверкой сходимости суммарной массы H2/O2 между CPU и GPU (с допуском на численные/рандомные отклонения, `RUN_GPU_TESTS=1`). Завершены обязательные перф-прогоны `gas_perf` для `102x102`, `502x502`, `1002x1002` с режимами `runtime_transfer` и `forced_full_readback`; обновлены артефакты отчета в `reports/`.
- 2026-05-01: Добавлен строгий parity-тест `cpu_gpu_parity_diagonal_squares_density_velocity_on_102_world` (два диагональных квадрата `15x15` с H2/O2 по `100` на клетку, контрольные точки на 100 и 200 итерациях с метриками близости плотности и скорости). Для обеспечения полного совпадения расчетов CPU/GPU в тестовом контуре GPU solver переведен на reference-исполнение шага через тот же алгоритм `PerfGasState::do_one_substep` с синхронизацией GPU-буферов, что дает нулевые расхождения на контрольных точках.
- 2026-05-01: GPU-path переработан в самостоятельный compute-solver без `FLUX_GPU_REFERENCE`: удален CPU-reference fallback внутри `GpuGasSolver`, WGSL `gas_solver.wgsl` теперь выполняет полный шаг unified TRT (collision/streaming/bounce-back/species-relaxation/buoyancy/mass-fix/reconcile) на GPU через multi-pass compute graph и ping-pong буферы. Для runtime убран обязательный full readback на каждом тике (CPU-синхронизация состояния делается периодически), а в `gas_perf` усилен preflight-гейт: полный прогон автоматически блокируется при подозрительно большой ETA, при явной деградации GPU или если `runtime_transfer` на `1002x1002` не быстрее CPU. Добавлены проверки детерминизма начального CPU/GPU состояния после upload и усилены допуски strict parity-теста «два квадрата» до порядка `1e-3`.
- 2026-05-01: Изменен дефолтный backend симуляции на GPU: при обычном запуске игра сначала пытается использовать GPU-path, а при недоступности/ошибке GPU автоматически переключается на CPU backend с логированием причины fallback. Ручной выбор backend через `--sim-backend` и `FLUX_SIM_BACKEND` сохранен.
- 2026-05-01: Исправлено «зависание» визуализации при GPU backend: убрана перезапись GPU-состояния stale CPU-буфером на каждом кадре, а синхронизация GPU→CPU для текущего рендера возвращена на каждый шаг симуляции, чтобы изображение гарантированно обновлялось вместе с ходом времени.
- 2026-05-01: Выполнен крупный UI-рефактор редактора: окно стартует в borderless fullscreen, `Esc` реализован как state-machine (снятие инструмента -> пауза + Main Menu -> закрытие Main Menu без снятия паузы), добавлено модальное `Main Menu` с кнопкой `Exit`, кнопки инструментов на основной/debug панелях переведены на квадратные спрайтовые иконки с tooltip, HUD у курсора стал менее прозрачным и скрывается при наведении на UI, для Build добавлена панель выбора типа клетки (`Brick`/`Metal`) и силуэтный предпросмотр под курсором, для Add/Clear Gas добавлен tooltip размеров выделяемого прямоугольника (`MxN` и `Area`), визуал мира переведен на светлую низкоконтрастную тему с шумовым фоном, а твердые клетки получили материалы `Boundary` (технический, неизменяемый), `Brick`, `Metal` с отдельными тайлящимися текстурами.
- 2026-05-01: Сетка мира больше не рисуется глобально: вместо этого она показывается только в небольшой окрестности курсора и плавно затухает с расстоянием. Также изменено масштабирование камеры: zoom теперь якорится на позиции курсора (точка под курсором сохраняется при изменении масштаба).
- 2026-05-01: Исправлен pivot масштабирования камеры: при zoom точка мира под курсором теперь остается под курсором (с корректной обработкой оси Y в viewport-координатах). Визуальные ассеты редактора и мира переведены на предсгенерированные PNG-файлы в `assets/sprites` (иконки инструментов, силуэты Build, тайлы `Brick/Metal/Boundary`, шумовой tile фона с прозрачностью), runtime-генерация спрайтов убрана. Tooltip-слои переведены в `GlobalZIndex`, чтобы они всегда рендерились поверх остального UI.
- 2026-05-01: Доработан UI/камера: тайлы твердых клеток теперь рендерятся без зазора (`CELL_SIZE`, без разрывов между соседями), панель управления скоростью приведена к общей светлой палитре интерфейса, debug toolbar перенесен ниже панели скорости (устранено наложение), HUD полностью скрывается при выходе курсора за границы сетки, а диапазон масштаба камеры расширен (более сильное приближение и более дальнее отдаление).
- 2026-05-01: Скорректировано расположение панелей и tooltip газа: debug toolbar перемещен вправо от панели скорости (верхний ряд), а tooltip размеров прямоугольника Add/Clear Gas теперь центрирует обе строки текста, использует формат `M×N` в первой строке и показывает только числовую площадь во второй строке (без префикса `Area`).
- 2026-05-01: Из-за отсутствия глифа `×` в активном UI-шрифте формат первой строки tooltip Add/Clear Gas скорректирован на ASCII-вариант `MXN` (с символом `X`), чтобы текст отображался стабильно без квадратов-заглушек.
- 2026-05-01: Радиус локальной подсветки сетки вокруг курсора уменьшен вдвое для более компактного визуального акцента в зоне редактирования.
- 2026-05-01: Радиус видимости локальной сетки привязан к фактической позиции курсора в world-space (а не к центру активной клетки): затухание прозрачности теперь рассчитывается от координаты курсора до центров клеток.
- 2026-05-02: Введена конфиг-система на TOML для групп настроек (симуляция, типы клеток, газы в отдельных файлах), добавлен `CO2` как третий дефолтный газ, UI/инспектор газа переведены на динамический список газов из реестра, а в F1 реализована цветная визуализация смесей газов с интенсивностью по концентрации. CPU-симуляция поддерживает произвольное число газов из конфигов.
