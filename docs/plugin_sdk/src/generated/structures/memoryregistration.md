<!-- generated by cargo xtask generate-plugin-sdk-docs; do not edit by hand -->

<!-- MemoryRegistration -->

# `MemoryRegistration`

<span class="sdk-badge sdk-badge-sdk">sdk</span> <span class="sdk-kind">struct</span>

Source: **Registrar** (`crates/flux_plugin_sdk/src/registrar.rs`). Generated group: **Structures**.

## Description

In-memory registration snapshot collected for built-in plugins.

## Declaration

```rust
pub struct MemoryRegistration
```

## Fields

| Field | Type | Description |
| --- | --- | --- |
| `substances` | Vec < [`SubstanceDescriptor`](../structures/substancedescriptor.md) > | `substances` field stored as `Vec < SubstanceDescriptor >` on `MemoryRegistration`. |
| `entities` | Vec < [`EntityDescriptor`](../structures/entitydescriptor.md) > | `entities` field stored as `Vec < EntityDescriptor >` on `MemoryRegistration`. |
| `overlays` | Vec < [`OverlayDescriptor`](../structures/overlaydescriptor.md) > | `overlays` field stored as `Vec < OverlayDescriptor >` on `MemoryRegistration`. |
| `overlay_materials` | Vec < [`OverlayMaterialDescriptor`](../structures/overlaymaterialdescriptor.md) > | `overlay_materials` field stored as `Vec < OverlayMaterialDescriptor >` on `MemoryRegistration`. |
| `tools` | Vec < [`ToolDescriptor`](../structures/tooldescriptor.md) > | `tools` field stored as `Vec < ToolDescriptor >` on `MemoryRegistration`. |
| `save_chunks` | Vec < [`SaveChunkDescriptor`](../structures/savechunkdescriptor.md) > | `save_chunks` field stored as `Vec < SaveChunkDescriptor >` on `MemoryRegistration`. |
| `subscriptions` | Vec < [`PluginEvent`](../enums/pluginevent.md) > | `subscriptions` field stored as `Vec < PluginEvent >` on `MemoryRegistration`. |

