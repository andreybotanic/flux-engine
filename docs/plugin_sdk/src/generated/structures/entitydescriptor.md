<!-- generated by cargo xtask generate-plugin-sdk-docs; do not edit by hand -->

<!-- EntityDescriptor -->

# `EntityDescriptor`

<span class="sdk-badge sdk-badge-sdk">sdk</span> <span class="sdk-kind">struct</span>

Source: **Descriptors** (`crates/flux_plugin_sdk/src/descriptors.rs`). Generated group: **Structures**.

## Description

One plugin-owned placeable entity.

## Declaration

```rust
pub struct EntityDescriptor
```

## Fields

| Field | Type | Description |
| --- | --- | --- |
| `id` | [`EntityKindId`](../structures/entitykindid.md) | `id` field stored as `EntityKindId` on `EntityDescriptor`. |
| `label` | String | `label` field stored as `String` on `EntityDescriptor`. |
| `icon_path` | String | `icon_path` field stored as `String` on `EntityDescriptor`. |
| `silhouette_path` | Option < String > | `silhouette_path` field stored as `Option < String >` on `EntityDescriptor`. |

