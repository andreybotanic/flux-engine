<!-- generated by cargo xtask generate-plugin-sdk-docs; do not edit by hand -->

<!-- EntitySnapshot -->

# `EntitySnapshot`

<span class="sdk-badge sdk-badge-sdk">sdk</span> <span class="sdk-kind">struct</span>

Source: **Descriptors** (`crates/flux_plugin_sdk/src/descriptors.rs`). Generated group: **Structures**.

## Description

Minimal entity snapshot returned by read APIs.

## Declaration

```rust
pub struct EntitySnapshot
```

## Fields

| Field | Type | Description |
| --- | --- | --- |
| `id` | [`EntityInstanceId`](../structures/entityinstanceid.md) | `id` field stored as `EntityInstanceId` on `EntitySnapshot`. |
| `kind` | [`EntityKindId`](../structures/entitykindid.md) | `kind` field stored as `EntityKindId` on `EntitySnapshot`. |
| `origin` | [`CellPos`](../structures/cellpos.md) | `origin` field stored as `CellPos` on `EntitySnapshot`. |
| `rotation` | [`Rotation`](../enums/rotation.md) | `rotation` field stored as `Rotation` on `EntitySnapshot`. |
| `enabled` | bool | `enabled` field stored as `bool` on `EntitySnapshot`. |
| `label` | Option < String > | `label` field stored as `Option < String >` on `EntitySnapshot`. |

