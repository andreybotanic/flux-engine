<!-- generated by cargo xtask generate-plugin-sdk-docs; do not edit by hand -->

<!-- FluxRuntimeHost::write_save_chunk -->

# `FluxRuntimeHost::write_save_chunk`

<span class="sdk-badge sdk-badge-ABI">ABI</span> <span class="sdk-kind">method</span>

Source: **ABI v4** (`src/plugins/abi.rs`). Generated group: **Methods**.

## Description

Writes one plugin-owned save chunk into the host chunk store.

## Signature

```rust
fn write_save_chunk (& mut self , chunk_id : & str , version : u32 , bytes : & [u8] ,) -> Result < () , FluxStatus >
```

## Arguments

| Argument | Type | Description |
| --- | --- | --- |
| `chunk_id` | & str | `chunk_id` argument passed as `& str`. |
| `version` | u32 | `version` argument passed as `u32`. |
| `bytes` | & [u8] | `bytes` argument passed as `& [u8]`. |

## Return Value

Result < () , [`FluxStatus`](../structures/fluxstatus.md) >

## SDK Example

_Source: [`examples/methods/fluxruntimehost-write-save-chunk.md`](../../examples/methods/fluxruntimehost-write-save-chunk.md)_

```rust
fn write_counter(host: &mut FluxRuntimeHost, counter: u32) -> FluxStatus {
    let payload = counter.to_le_bytes();
    match host.write_save_chunk("flux.demo.save.counter", 1, &payload) {
        Ok(()) => FluxStatus::OK,
        Err(status) => status,
    }
}
```

